# Kubernetes Resource Allocation & Limits

Deploying microservices with strict cluster compute profiles:
- Deployment targets: ClusterIP internal service configurations (`k8s/services.yaml`).
- Storage configurations: persistence setup for payment database.
- Resource limits block: limits CPU and memory scaling ranges.
