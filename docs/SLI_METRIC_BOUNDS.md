# Service Level Indicators (SLI) Bounds

Standard parameter boundaries for healthy services operations:
- CPU usage percentage: target range $\le 80.0\%$.
- Response latency: Target $P_{95} \le 200.0$ milliseconds.
- HTTP error rates: target bounds $\le 1.0\%$ of requests.
